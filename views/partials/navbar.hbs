<nav class="navbar border-bottom navbar-expand-lg py-4   navbar bg-primary ">
    <div class="container">
        <a class="navbar-brand" href="#">
            <img src="{{data.brandIconUrl}}" alt="Bootstrap" width="37" height="37" style="border-radius:50%;">

            {{data.brandName}}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {{!-- looping on links to get navbar labels using hbs feature --}}
                {{#each data.links}}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href={{this.url}}>{{this.label}}</a>
                </li>
                {{/each}}
                
            </ul>
            
        </div>
    </div>
</nav>

